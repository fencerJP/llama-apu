You are a principal deep learning research scientist and neural compiler engineer specializing in modern autoregressive sequence modeling and hardware-efficient transformer variants.

Please produce a comprehensive comparative mathematical and architectural dissertation analyzing the mathematical foundations, memory access patterns, and hardware mapping efficiency of four distinct modern model families:
1. Standard Scaled Dot-Product Attention Transformers (e.g. Meta Llama 3.2, Qwen 2.5):
   - Formulate the standard self-attention mechanism: Attention(Q, K, V) = softmax(Q K^T / sqrt(d_k)) V.
   - Explain why the quadratic computational complexity O(N^2) in sequence length N creates severe Time-To-First-Token (TTFT) latency overhead during the prompt prefill phase on memory-constrained client devices.
   - Detail the Key-Value (KV) cache memory growth during sequential autoregressive decoding, and explain why memory bandwidth rather than peak arithmetic throughput becomes the primary roofline bottleneck on mobile APUs.

2. Gated Delta Linear Attention / GateDeltaNet (e.g. Qwen 3.5, Ornith 9B):
   - Contrast quadratic softmax attention with linear attention formulations where the softmax operator is replaced with feature maps or recurrent delta update rules: S_t = S_{t-1} + v_t k_t^T.
   - Derive how the delta rule enables the model to selectively update and forget information within a fixed-size state matrix, achieving strictly O(1) memory complexity during decode steps.
   - Analyze why the inclusion of 1D depthwise convolutions across sequence dimensions requires specialized NPU kernel scheduling (such as conv.xclbin kernels) to avoid spatial tile pipeline stalls on AMD XDNA 2.

3. Selective State Space Models / SSMs (e.g. Mamba-2, Spark-X2.5):
   - Formulate the continuous and discretized state-space equations: h'(t) = A h(t) + B x(t), y(t) = C h(t) + D x(t).
   - Explain the hardware advantage of the parallel associative scan during the prefill phase, and contrast its arithmetic intensity against standard GEMM operators on RDNA 3.5 iGPUs.
   - Describe how the hybrid interleaving of SSM layers with multi-head attention (as found in Spark-X2.5) balances long-context associative recall with constant-memory recurrent inference.

4. Liquid Neural Networks and Continuous-Time Dynamics (e.g. Liquid Foundation Models LFM-2):
   - Detail the formulation of adaptive time-constant differential equations in liquid networks where synaptic weights and state transitions adapt dynamically to input sequence variance.
   - Discuss the challenges and compiler optimization strategies required to map liquid neural ODE state transitions onto 2D spatial systolic arrays without incurring high context-switch latency.

Conclude with a structured comparison table summarizing: Theoretical Flops per token, KV Memory Complexity during decode, Prefill compute intensity, and Optimal Hardware accelerator mapping (iGPU vs NPU) for each architecture family.
